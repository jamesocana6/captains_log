<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Captain's Log</title>
</head>
<body>
    <h1>All Logs</h1>
    <ul>
        <% logs.forEach(log => { %>
            <li>
                <a href="/logs/<%=log._id%>"><%=log.title%></a>
                <a href="/logs/<%=log._id%>/edit">Edit</a>
                <form style="display: inline; margin-left: 10px;" action="/logs/<%=log._id%>?_method=DELETE" method="POST">
                    <button>DELETE</button>
                </form>
            </li>
        <% }) %>
    </ul>
    <nav>
        <a href="/logs/new"><input type="button" value="New Log"></a>
    </nav>
</body>
</html>